# ChatGPT 학습 포인트 최적화 워크플로우

이 문서는 ChatGPT 학습 포인트를 체계적으로 관리하고 최적화하는 전체 워크플로우를 설명합니다.

## 📁 파일 구조

```
프로젝트 루트/
├── ChatGPT-Learning-Point.md          # 학습 포인트 작성 문서
├── config/
│   ├── llm-prompt-config.json         # 실제 적용되는 Config 파일
│   ├── LLM_CONFIG_GUIDE.md            # 상세 매핑 가이드
│   └── LEARNING_POINT_WORKFLOW.md     # 이 문서
└── scripts/
    └── validate-learning-points.ts    # 검증 스크립트
```

## 🔄 워크플로우

### 1단계: 학습 포인트 작성

`ChatGPT-Learning-Point.md` 파일을 열어 각 섹션에 학습 포인트를 작성합니다.

**작성 예시:**
```markdown
## 3. 작성 가이드라인 (writing_guidelines)

### 금지 사항 (prohibited)
- 감정적 위로
- 추상적 동기부여
- [추가] "당신은 특별합니다"
```

### 2단계: 검증

작성한 내용을 검증합니다:

```bash
npm run validate:learning-points
```

**검증 항목:**
- ✅ 필수 섹션 존재 여부
- ✅ Config 파일 구조 일관성
- ⚠️ 빈 섹션 경고

### 3단계: Config 파일에 반영

`config/LLM_CONFIG_GUIDE.md`의 매핑 가이드를 참고하여 `config/llm-prompt-config.json`에 반영합니다.

**매핑 예시:**

| ChatGPT-Learning-Point.md | llm-prompt-config.json |
|--------------------------|----------------------|
| `### 금지 사항` → `[추가] "당신은 특별합니다"` | `writing_guidelines.prohibited` 배열에 추가 |

**반영 예시:**
```json
{
  "writing_guidelines": {
    "prohibited": [
      "감정적 위로",
      "추상적 동기부여",
      "당신은 특별합니다"  // ← 추가
    ]
  }
}
```

### 4단계: 서버 재시작

변경사항을 적용하기 위해 서버를 재시작합니다:

```bash
# 개발 환경
npm run dev

# 프로덕션
npm start
```

### 5단계: 테스트 및 검증

실제 진단 결과를 확인하고 필요시 추가 조정합니다.

## 📋 섹션별 매핑 가이드

### 빠른 참조표

| ChatGPT-Learning-Point.md 섹션 | Config 필드 경로 | 반영 방법 |
|-------------------------------|----------------|----------|
| 1. 역할 및 전문성 → 역할 설명 | `role.description` | 직접 복사 |
| 1. 역할 및 전문성 → 전문 분야 | `role.expertise` | 배열 항목 추가 |
| 2. 진단 원칙 → 핵심 철학 | `diagnosis_principles.core_philosophy` | 배열 항목 추가 |
| 2. 진단 원칙 → 사고 패턴 | `diagnosis_principles.thinking_patterns` | 배열 항목 추가 |
| 3. 작성 가이드라인 → 스타일 | `writing_guidelines.style` | 객체 값 설정 |
| 3. 작성 가이드라인 → 금지 사항 | `writing_guidelines.prohibited` | 배열 항목 추가 |
| 3. 작성 가이드라인 → 필수 사항 | `writing_guidelines.required` | 배열 항목 추가 |
| 4. 섹션별 가이드라인 → 공통고민 | `section_guidelines.common_concerns` | 객체 설정 |
| 4. 섹션별 가이드라인 → 현재역량 | `section_guidelines.current_capabilities` | 객체 설정 |
| 4. 섹션별 가이드라인 → 학습포인트 | `section_guidelines.learning_points` | 객체 설정 |
| 5. 좋은 예시 → 공통고민 예시 | `examples.good_example.common_concerns` | 문자열 설정 |
| 5. 좋은 예시 → 현재역량 예시 | `examples.good_example.current_capabilities` | 문자열 설정 |
| 5. 좋은 예시 → 학습포인트 예시 | `examples.good_example.learning_points` | 문자열 설정 |
| 6. 나쁜 예시 → 피해야 할 표현 | `examples.bad_example.examples` | 배열 항목 추가 |
| 7. 컨텍스트 사용 규칙 → 규칙 | `context_usage.rules` | 배열 항목 추가 |
| 8. 출력 형식 검증 → 금지 단어 | `output_format.validation.prohibited_words` | 배열 항목 추가 |
| 8. 출력 형식 검증 → 문장 수 제한 | `output_format.validation.sentence_count_per_section` | 객체 설정 |

## 🎯 최적화 팁

### 1. 점진적 개선
- 한 번에 많은 변경보다는 작은 변경을 여러 번 테스트
- 변경 후 실제 결과를 확인하며 조정

### 2. 예시 활용
- 좋은 예시는 실제로 원하는 결과와 유사하게 작성
- 나쁜 예시는 자주 발생하는 문제를 명확히 제시

### 3. 일관성 유지
- 모든 섹션에서 동일한 톤과 스타일 유지
- 금지 사항과 필수 사항이 충돌하지 않도록 주의

### 4. 검증 활용
- 매번 Config 파일 수정 전후로 검증 스크립트 실행
- JSON 문법 오류를 사전에 방지

## 🔍 문제 해결

### Config가 적용되지 않는 경우

1. **서버 재시작 확인**
   - Config는 서버 시작 시 로드되므로 재시작 필요

2. **JSON 문법 오류 확인**
   - `npm run validate:learning-points` 실행
   - 온라인 JSON 검증 도구 사용

3. **파일 경로 확인**
   - `config/llm-prompt-config.json` 경로가 정확한지 확인

### 원하는 결과가 나오지 않는 경우

1. **예시 업데이트**
   - 좋은 예시를 더 구체적으로 작성
   - 나쁜 예시에 문제가 되는 표현 추가

2. **가이드라인 강화**
   - 금지 사항에 문제 표현 추가
   - 필수 사항에 원하는 내용 추가

3. **점진적 조정**
   - 한 번에 많은 변경보다는 작은 변경을 여러 번 테스트

## 📚 관련 문서

- **ChatGPT-Learning-Point.md**: 학습 포인트 작성 템플릿
- **config/LLM_CONFIG_GUIDE.md**: 상세 매핑 가이드 및 Config 구조 설명
- **config/llm-prompt-config.json**: 실제 적용되는 Config 파일

## 🚀 빠른 시작

1. `ChatGPT-Learning-Point.md` 파일 열기
2. 학습 포인트 작성
3. `npm run validate:learning-points` 실행
4. `config/LLM_CONFIG_GUIDE.md` 참고하여 Config 파일에 반영
5. 서버 재시작
6. 테스트 및 검증

---

**마지막 업데이트**: 2024년
**다음 검토 예정일**: 

